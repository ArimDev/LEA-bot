import { klipy, fotosoutez } from "../../src/functions/images.js";

export default async function (bot, m) {
    //OUT OF ORDER
    //if (m.channel.id === "1139268236806991933") await klipy(bot, m);
    //if (m.channel.id === "1241826436525064372") await fotosoutez(bot, m);
}